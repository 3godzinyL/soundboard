{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the soundboard app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
